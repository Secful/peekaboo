# Visual Crawler — Claude Code Instructions

## Deployment

- Deploy script: `bash scripts/deploy.sh`
- Always run the deploy script when asked to deploy — no confirmation needed
- Deploys to AWS ECS Fargate (Docker build → ECR push → ECS service update)
